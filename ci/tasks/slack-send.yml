---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: governmentpaas/git-ssh

inputs:
- name: slack-payload
- name: branch

run:
  path: ci/scripts/slack-send
  dir: branch