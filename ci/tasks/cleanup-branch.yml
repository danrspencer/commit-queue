---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: governmentpaas/git-ssh

inputs:
- name: commit-queue

run:
  dir: commit-queue
  path: ci/scripts/setup-ssh-and
  args: [ cleanup-branch ]
