---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: governmentpaas/git-ssh

inputs:
- name: commit-queue

outputs:
- name: slack-payload

run:
  dir: commit-queue/scripts
  path: setup-ssh-and
  args: [ slack-generate ]