---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: governmentpaas/git-ssh

inputs:
- name: commit-queue

outputs:
- name: slack-payload

run:
  path: commit-queue/scripts/setup-ssh